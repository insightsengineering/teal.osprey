# Validate module `transformators` argument

Validate module `transformators` argument

## Usage

``` r
assert_transformators(transformators)
```

## Arguments

- transformators:

  (`list` or `teal_transform_module`)
