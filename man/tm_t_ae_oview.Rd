% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_t_ae_oview.R
\name{tm_t_ae_oview}
\alias{tm_t_ae_oview}
\title{Adverse Events Summary Table Teal Module}
\usage{
tm_t_ae_oview(label, dataname, arm_var, arm_var_choices, total_col = TRUE,
  pre_output = NULL, post_output = NULL, code_data_processing = NULL)
}
\arguments{
\item{label}{menu item label of the module in the teal app}

\item{dataname}{analysis data used in teal module, needs to be available in
the list passed to the \code{data} argument of \code{\link[teal]{init}}.
Note that the data is expected to be in vertical form with the
\code{PARAMCD} variable filtering to one observation per patient.}

\item{arm_var}{single name of variable in analysis data that is used as
\code{col_by} argument for the respective \code{tern} function.}

\item{arm_var_choices}{vector with variable names that can be used as
\code{arm_var}}

\item{total_col}{argument for appearance of All Patients column,
default here is TRUE}

\item{pre_output}{optional, \code{shiny.tag} with text placed before the
output to put the output into context. For example the
\code{\link[shiny]{helpText}} elements are useful.}

\item{post_output}{optional, \code{shiny.tag} with text placed before the
output to put the output into context. For example the
\code{\link[shiny]{helpText}} elements are useful.}
}
\value{
an \code{\link[teal]{module}} object
}
\description{
Display AET01 Adverse Events Summary Table as a shiny Module
}
\examples{

\dontrun{
#Example using stream (adam) dataset 
library(dplyr)

data("rADSL")
data("rADAE")

ASL <- rADSL
AAE <- rADAE

x1 <- teal::init(
  data = list(ASL = ASL, AAE = AAE),
  modules = root_modules(
    tm_t_ae_oview(
       label = "AE Overview Summary Table",
       dataname = "AAE",
       arm_var = "ARM",
       arm_var_choices = c("ARM", "ARMCD"),
       total_col = FALSE
   )
  )
)
   
shinyApp(x1$ui, x1$server)

}

  
}
\author{
Carolyn Zhang (zhanc107) \email{carolyn.zhang@duke.edu}

Chendi Liao (liaoc10) \email{chendi.liao@roche.com}
}
